#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cssstats/bin/cssstats.js" "$@"
  ret=$?
else 
  node  "$basedir/../cssstats/bin/cssstats.js" "$@"
  ret=$?
fi
exit $ret
